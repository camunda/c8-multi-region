---
name: Renovate Auto Merge

on:
    pull_request:

# limit to a single execution per ref of this workflow
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    renovate-automerge:
        uses: camunda/infraex-common-config/.github/workflows/automerge-global.yml@0f44c132019ad2023661e1d747a264f54acd563d # 1.3.3
        secrets: inherit
